<nav class="bg-white border-b border-neutral-200 sticky top-0 z-50" data-controller="mobile-nav">
  <div class="px-6">
    <div class="flex items-center justify-between h-16">
      <!-- Logo/Brand -->
      <%= render "layouts/app_logo" %>

      <!-- User Menu & Actions -->
      <div class="flex items-center space-x-4">

        <!-- Add New Button -->
        <%= render "layouts/add_new_button" %>

        <!-- User Menu Placeholder -->
        <div class="hidden sm:flex items-center space-x-3">
          <%= link_to "Sign Out", "#", class: "px-4 py-2 text-sm font-medium text-neutral-700 hover:text-neutral-900 transition-colors" %>
        </div>

        <!-- Mobile Menu Button -->
        <%= render "layouts/mobile_menu_button" %>
      </div>
    </div>
  </div>

  <!-- Mobile Navigation Menu -->
  <%= render "layouts/mobile_nav_menu" %>
</nav>
