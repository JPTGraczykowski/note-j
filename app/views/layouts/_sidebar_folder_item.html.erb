<% depth ||= 0 %>
<% all_folders ||= @all_folders %>

<%= turbo_frame_tag dom_id(folder) do %>
  <div data-folder-tree-target="folder" data-folder-id="<%= folder.id %>">
    <% children = children_for_folder(folder, all_folders) %>
    <div class="flex items-center group relative" style="padding-left: <%= depth * 12 %>px;">
      <% if children.any? %>
        <button
          type="button"
          data-folder-tree-target="toggle"
          data-folder-id="<%= folder.id %>"
          data-action="click->folder-tree#toggle"
          class="w-6 h-6 flex items-center justify-center text-neutral-400 hover:text-neutral-600 transition-colors flex-shrink-0 -ml-1 hover:cursor-pointer">
          <%= render "shared/svg/chevron_right" %>
        </button>
      <% else %>
        <span class="w-5 flex-shrink-0"></span>
      <% end %>

      <%= link_to notes_path(folder_id: folder.id),
          data: { turbo_frame: "_top" },
          class: "flex items-center flex-1 px-3 py-2 text-sm rounded-md transition-colors min-w-0 #{params[:folder_id]&.to_i == folder.id ? 'bg-primary-100 text-primary-700' : 'text-neutral-600 hover:text-neutral-900 hover:bg-neutral-100'}" do %>
        <%= render "shared/svg/folder" %>
        <span class="flex-1 truncate"><%= folder.name %></span>
        <span class="ml-auto text-xs bg-neutral-200 text-neutral-700 px-2 py-0.5 rounded-full flex-shrink-0">
          <%= folder.notes_count %>
        </span>
      <% end %>

      <div class="hidden group-hover:flex items-center gap-1 absolute right-2 bg-neutral-50">
        <%= link_to edit_folder_path(folder),
            data: { turbo_frame: dom_id(folder) },
            class: "p-1 text-neutral-400 hover:text-primary-600 transition-colors",
            title: "Edit folder" do %>
          <%= render "shared/svg/edit" %>
        <% end %>

        <%= button_to folder_path(folder),
            method: :delete,
            data: {
              turbo_frame: "_top",
              turbo_confirm: "Are you sure you want to delete this folder? All notes in this folder and subfolders will be deleted permanently."
            },
            class: "p-1 text-neutral-400 hover:text-red-600 transition-colors",
            title: "Delete folder" do %>
          <%= render "shared/svg/trash" %>
        <% end %>
      </div>
    </div>

    <div data-folder-tree-target="children" class="mt-1 <%= 'hidden' unless children.any? %>" id="folder_<%= folder.id %>_children">
      <% children.each do |child_folder| %>
        <%= render 'layouts/sidebar_folder_item', folder: child_folder, depth: depth + 1, all_folders: all_folders %>
      <% end %>
    </div>
  </div>
<% end %>
